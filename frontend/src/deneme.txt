COMPONENTS
  1-Header
    -Header: Sabit Header
    -ProfileMenu: Profil sayfası olacak (yapılmadı)

  2-Process
    -ProcessAdd: Process ekleme ekranı -path: /add-process

  3-Project
    -ProjectCartSelectModal: Proje kartına tıklandığında açılan Modal
    -ProjectTasks: Projeye ait tüm görevlerin gösterildiği Ekran (ProjectCartSelectModal'da "Görevleri Görüntüle" butonuna tıklandığında açılır) -path: /projectTasks
    -ProjectProcessSelectModal: Projeye eklenecek süreçlerin seçildiği ekran (ProjectCartSelectModal'da "Süreç Oluştur" butonuna tıklandığında açılır)

  4-Tasks
    -TaskAdd: Task Ekleme Ekranı -path: /add-task

LAYOUT
  -AdminDashboard: Ana ekran div'i gibi düşünebiliriz. Route sayfasında sidebar içerecek tüm sayfalar bu sayfanın altına eklenir.

PAGES
  -Login: Giriş Sayfası
  -MyTasks: Görevlerim sayfası
  -Processes: Süreçler Sayfası
  -ProjectCreated: Proje Ekleme Sayfası
  -Projects: Projelerin görüntülendiği sayfa



// props olarak modal'da mı çalışıyoruz kontrolü
const ProcessAdd = ({ isModal = false, closeModal }) => {
  ...
  return (
    <div className={isModal ? "" : "min-h-screen bg-gradient-to-br from-gray-50 to-blue-50"}>
      {!isModal && <Header title="Süreç Yönetimi" />}
      ...
      {/* Kaydet butonu */}
      <Button
        onClick={handleConfirm}
        loading={isLoading}
        className="bg-gradient-to-r from-ivosis-600 to-ivosis-700 ..."
        size="md"
      >
        Kaydet
      </Button>
      ...
      {/* Başarı Modal - Eğer modaldaysa otomatik kapat */}
      <Modal
        opened={showSuccess}
        onClose={() => {
          setShowSuccess(false);
          if (isModal && closeModal) closeModal(); // modalı kapat
        }}
        ...
      >
        ...
      </Modal>
      ...
    </div>
  );
};


Sicin No: kendisi verecek otomatik artacak
Name: text
surname: text
title: text
badge: select
department: select
section: select - enerji çelik
startDate - date
BirthPlace  - select (şehir listesi)
birthDate - date
Tc kimlik - text
educationalLevel - select
gender - select
nationality - text
city - select (şehirler)
district - select (ilçeler)
adress -text area
mobilePhone - text
email - text
salary - text
IBAN - text
photo - file
workstatus - select (aktif pasif çalışma durumu) 




ekle butonları daha dar olacak +

personel listesi detay modal tasarımı düzenlenecek +
personel düzenle modal'ı personel ekle sayfası gibi olacak +
sil butonu gizlenecek kodlar kalacak +
ekle sayfasındaki footer sabitlenecek modalda da aynı yapı olacak +
ekle sayfasındaki header enable ayarları düzenlenecek

belgeler indirilme sorunu var
Proje düzenleme ekranı tasarlanacak

TaskListModal'da düzenle ve silme işlemleri çalışmıyor
TaskAddModal ekleme işlemi çalışmıyor.

Personel Düzenle modalinde düzenle butonu çalışmıyor. (backende update fonksiyonundaki alan kontrolleri tc ve email kontrol edilecek)

proje kartına adres detayı eklenecek
proje eklerken proje tipi eklenecek
süreç ekleme modalı açıldığında imleç texte odaklanacak
sayfalardaki görüntüleme sayısı seçilebilir olmalı
dosya ekleme sayısının kontrolü
dosya yüklenince sıralama yapılacak
süreçlerde ve taskta bitiş tarihi de atanacak
task düzenlede tüm değişkenler düzenlenecek
süreç kartı düzenlenecek(çok yazı olunca kayma oluyor)
görevlerde dosya alanı boşsa tamamlandı olamaz şekilde ayarlanacak

