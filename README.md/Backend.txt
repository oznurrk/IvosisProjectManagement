               ┌──────────────┐
               │  Client (UI) │
               └──────┬───────┘
                      │
           sends DTO  ▼
              ┌────────────────────┐
              │   Controller       │
              │ (UserCreateDto)    │
              └──────┬─────────────┘
                     │ uses
                     ▼
            ┌────────────────┐
            │   AutoMapper   │
            └──────┬─────────┘
                   ▼
              ┌─────────┐
              │ Model   │
              │ (User)  │
              └────┬────┘
                   ▼
             ┌────────────┐
             │ Service    │
             │ (EF Core)  │
             └────────────┘




git pull origin main
# (Kodlarını yaz/düzenle)
git status
git add .
git commit -m "Günlük değişiklik: XYZ işlemleri yapıldı"
git push origin main




-----------------

git add .
git commit -m "your message"
git pull 

----------------------------------------------------------
git add .
git commit -m "Backend: ProjeController güncellendi"
git push origin backend


git checkout main
git pull origin main
git merge backend     # veya arkadaşın için: git merge frontend
git push origin main
